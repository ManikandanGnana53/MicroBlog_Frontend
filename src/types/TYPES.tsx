export interface RegisterProps {
    firstName:string,
    lastName:string,
    email: string,
    password: string,
    contact: number,
}
export interface LoginTypes {
    email: string,
    password: string,
    accessToken: string
}